
export interface Project {
  title: string;
  description: string;
  imageUrl: string;
  stats?: string;
}

export interface Service {
  name: string;
  icon?: string;
}

export interface Tool {
  name: string;
  icon?: string;
}
